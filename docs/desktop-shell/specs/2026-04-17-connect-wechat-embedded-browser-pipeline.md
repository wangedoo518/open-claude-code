# Connect 微信客服 · 嵌入式浏览器 Pipeline 设计方案

> **参考**: Replit Publishing Pipeline UX + Obsidian Custom Frames 嵌入模式
> **日期**: 2026-04-17
> **版本**: v2.0（替代纯 Modal 方案）
> **状态**: 团队评审

---

## 一、核心洞察

### Replit 部署流程的 UX 精髓（截图分析）

Replit 的 Publishing 流程有三个值得学习的模式：

| 模式 | Replit 实现 | 价值 |
|------|-----------|------|
| **顶部进度条** | Provision → Security Scan → Build → Bundle → Promote，绿色依次点亮 | 用户始终知道"到哪一步了" |
| **可展开日志** | "View logs" / "Hide logs" 切换，日志实时滚动 | 想看细节可以看，不想看不打扰 |
| **完成弹窗** | "Congrats! Use the link below to share" + 复制链接 | 清晰的成功信号 + 立即可用的结果 |

### Obsidian Custom Frames 的嵌入模式

关键技术发现：
- **Electron/Tauri 桌面端用 webview**，有完整浏览器能力（Cookie、登录、表单）
- **ClaudeWiki 已有 MinApp 基础设施**（`minapps.ts` + `webviewStateManager.ts`），可直接复用
- **webview 可以嵌入 Cloudflare Dashboard 和微信客服后台**，用户在 App 内完成所有操作

### 关键改进：从"弹窗"到"嵌入式工作台"

v1 方案（纯 Modal）的问题：
- Pipeline 执行时用户只能看到进度条 + 日志，无法看到浏览器操作过程
- Cloudflare 注册、微信授权需要跳转到外部浏览器，打断用户流程
- 用户不知道"自动化在做什么"

v2 方案（嵌入式浏览器）的改进：
- **左侧 Pipeline 进度 + 右侧嵌入浏览器**，用户实时看到自动化操作
- Cloudflare 注册/部署 → 在 App 内嵌的 webview 中完成
- 微信客服授权 → 在 App 内嵌的 webview 中扫码
- 全程不离开 ClaudeWiki

---

## 二、产品架构

```
┌─────────────────────────────────────────────────────────────────┐
│  Connect 微信客服 · 嵌入式 Pipeline                              │
│                                                                 │
│  ┌──── 左侧 Pipeline 面板 ────┐  ┌──── 右侧嵌入浏览器 ────────┐  │
│  │                            │  │                             │  │
│  │  ① CF 账号      ✅ 已完成   │  │  ┌─────────────────────┐   │  │
│  │  ② 部署中继     ✅ 已完成   │  │  │                     │   │  │
│  │  ③ 微信授权     🔄 执行中   │  │  │   Cloudflare /      │   │  │
│  │  ④ 回调配置     ○ 待执行   │  │  │   微信后台 /         │   │  │
│  │  ⑤ 创建账号     ○ 待执行   │  │  │   QR 扫码页          │   │  │
│  │                            │  │  │                     │   │  │
│  │  ─────────────────────     │  │  │  （webview 实时渲染） │   │  │
│  │  📋 执行日志                │  │  │                     │   │  │
│  │  [15:03] 部署 Worker ✓     │  │  └─────────────────────┘   │  │
│  │  [15:05] 等待扫码...       │  │                             │  │
│  │                            │  │  ◀ ▶ 🔄  dash.cloudflare.. │  │
│  └────────────────────────────┘  └─────────────────────────────┘  │
│                                                                   │
│  ┌─────── 顶部进度条（Replit 风格）──────────────────────────────┐  │
│  │ [■■■ CF 账号 ✓] [■■■ 部署中继 ✓] [■■■ 微信授权 ◎] [○ 回调] [○] │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、用户旅程（完整流程）

```
Step 0: 侧边栏点击"微信助手 · 未连接"
    │
    ▼
Step 1: ConnectWeChatModal 选择方式
    ├── 🚀 一键接入 → 进入嵌入式 Pipeline 页面（全屏）
    └── ⚙ 手动配置 → 表单 Modal（保持不变）
    │
    ▼
Step 2: 嵌入式 Pipeline 页面（替代原来的 Modal 进度）
    │
    ├── Phase ①: Cloudflare 账号
    │   ├── 用户已有 token → 跳过
    │   └── 用户没有 → 右侧 webview 打开 dash.cloudflare.com/sign-up
    │       用户在 webview 内注册，完成后自动检测
    │
    ├── Phase ②: 部署中继服务器
    │   └── 右侧 webview 显示 Cloudflare Workers 部署页面
    │       自动执行 wrangler deploy，日志实时滚动
    │
    ├── Phase ③: 微信客服授权
    │   └── 右侧 webview 打开 kf.weixin.qq.com
    │       显示 QR 码，用户用手机微信扫码
    │       扫码成功后自动检测并进入下一步
    │
    ├── Phase ④: 回调 URL 配置
    │   └── 右侧 webview 显示微信后台回调配置页
    │       自动填入 Worker URL，用户确认
    │
    └── Phase ⑤: 创建客服账号
        └── 自动调用 API，右侧 webview 显示成功页
    │
    ▼
Step 3: 完成页（Replit 风格 Congrats 弹窗）
    ├── ✅ 微信客服已连接
    ├── 客服二维码 + 复制链接
    ├── 使用方法提示
    └── [完成] 按钮 → 返回主界面
```

---

## 四、交互设计线框图

### 4.1 Step 1: 选择连接方式（保持不变）

与 v1 ConnectWeChatModal Step 1 相同。

### 4.2 Step 2: 嵌入式 Pipeline 页面（核心新设计）

这是一个**全屏路由页面**（不是 Modal），占据整个主内容区。

```
┌─ 顶栏 ──────────────────────────────────────────────────────────────┐
│  ◀ 返回                    一键接入微信客服                    取消  │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─── 进度条（Replit 风格）─────────────────────────────────────────┐ │
│  │ [■■■ CF 账号 ✓][■■■ 部署中继 ✓][■■■ 微信授权 ◎][○ 回调][○ 创建] │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌─── 左侧 Pipeline 面板 (320px) ──┐  ┌─── 右侧嵌入浏览器 ───────┐ │
│  │                                  │  │                           │ │
│  │  ③ 扫码授权企微           🔄     │  │ ◀ ▶ 🔄 🔗                 │ │
│  │                                  │  │ kf.weixin.qq.com/...      │ │
│  │  请用手机微信扫描右侧二维码       │  │ ┌───────────────────────┐ │ │
│  │  授权 ClaudeWiki 接入你的         │  │ │                       │ │ │
│  │  企业微信客服                     │  │ │                       │ │ │
│  │                                  │  │ │     微信客服后台        │ │ │
│  │  ─────────────────────────       │  │ │     QR 扫码页面        │ │ │
│  │                                  │  │ │                       │ │ │
│  │  已完成步骤:                      │  │ │  （webview 实时渲染）   │ │ │
│  │  ✅ ① Cloudflare 账号   已跳过   │  │ │                       │ │ │
│  │  ✅ ② 部署中继服务器     已完成   │  │ │                       │ │ │
│  │  🔄 ③ 扫码授权企微      等待扫码  │  │ │                       │ │ │
│  │  ○ ④ 回调 URL 配置     待执行    │  │ └───────────────────────┘ │ │
│  │  ○ ⑤ 创建客服账号      待执行    │  │                           │ │
│  │                                  │  │                           │ │
│  │  ─────────────────────────       │  │                           │ │
│  │  📋 执行日志         [展开 ▼]    │  │                           │ │
│  │  ┌────────────────────────┐      │  │                           │ │
│  │  │ [15:03:01] 跳过 CF 注册│      │  │                           │ │
│  │  │ [15:03:03] 部署 Worker │      │  │                           │ │
│  │  │ [15:03:05] 等待扫码... │      │  │                           │ │
│  │  └────────────────────────┘      │  │                           │ │
│  │                                  │  │                           │ │
│  │              [取消接入]           │  │                           │ │
│  └──────────────────────────────────┘  └───────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

#### 关键交互细节

**进度条行为**（参考 Replit）：
- 5 个阶段块，已完成的变绿 + ✓，当前执行的变蓝/橙 + 旋转图标，待执行的灰色
- 点击已完成的阶段块 → 左侧面板切换到该步骤的日志
- 当前执行阶段的块有微弱脉冲动画

**右侧 webview 的 URL 切换**：

| Pipeline 阶段 | webview 加载的 URL |
|-------------|-------------------|
| ① CF 账号 | `https://dash.cloudflare.com/sign-up` (或跳过) |
| ② 部署中继 | `https://dash.cloudflare.com/workers` (显示部署过程) |
| ③ 微信授权 | `https://kf.weixin.qq.com/` (扫码页面) |
| ④ 回调配置 | 微信后台回调配置页（自动填入 Worker URL） |
| ⑤ 创建账号 | 完成占位页（显示"正在创建客服账号..."） |

**webview 导航栏**：
```
┌─ webview 工具栏 ─────────────────────────────────────┐
│ ◀ ▶ 🔄  │  kf.weixin.qq.com/...        │ 🔗 在浏览器打开 │
└──────────────────────────────────────────────────────┘
```
- ◀ ▶ 前进后退
- 🔄 刷新
- 🔗 在外部浏览器打开（如果 webview 有问题，用户可以降级）

### 4.3 Step 3: 完成页（Replit Congrats 风格）

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  ┌─── 进度条 ──────────────────────────────────────────────────────┐ │
│  │ [■■■ CF ✓] [■■■ 部署 ✓] [■■■ 授权 ✓] [■■■ 回调 ✓] [■■■ 创建 ✓] │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│                          🎉                                          │
│                                                                      │
│                 微信客服接入完成！                                     │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │                                                              │    │
│  │   ┌────────────┐     ClaudeWiki 助手                         │    │
│  │   │  QR Code   │                                            │    │
│  │   │            │     扫码添加到微信                            │    │
│  │   │            │                                            │    │
│  │   └────────────┘     中继: relay.cw-xxx.workers.dev          │    │
│  │                      监听: ● 运行中                           │    │
│  │                                                              │    │
│  │   ┌──────────────────────────────────────┐                   │    │
│  │   │ https://work.weixin.qq.com/kfid/...  │  📋 复制链接       │    │
│  │   └──────────────────────────────────────┘                   │    │
│  └──────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─ 使用方法 ──────────────────────────────────────────────────────┐ │
│  │  · 发送链接 → 自动入库        · ?提问 → 查询知识库               │ │
│  │  · 发送文本 → 记录笔记        · /recent → 最近摄入               │ │
│  │  · 发送文件 → 文件提取        · /stats → 知识统计                │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│                        [完成，回到主页 →]                             │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 五、技术方案

### 5.1 webview 嵌入（复用 MinApp 基础设施）

ClaudeWiki 已有 MinApp webview 机制（`minapps.ts` + `webviewStateManager.ts`），
Pipeline 页面的右侧浏览器直接复用此机制：

```typescript
// Pipeline webview 不注册为 MinApp，而是作为 PipelinePage 的内部组件
// 使用 Tauri 的 <webview> 或降级为 <iframe>

interface PipelineWebviewProps {
  url: string;           // 当前阶段要加载的 URL
  onUrlChange?: (url: string) => void;  // URL 变化回调
}

function PipelineWebview({ url }: PipelineWebviewProps) {
  return (
    <div className="flex flex-col h-full">
      {/* 导航栏 */}
      <div className="flex items-center h-9 border-b px-2 gap-1">
        <button>◀</button>
        <button>▶</button>
        <button>🔄</button>
        <span className="flex-1 truncate text-xs">{url}</span>
        <button>🔗</button>
      </div>
      {/* webview/iframe */}
      <webview src={url} className="flex-1" />
    </div>
  );
}
```

### 5.2 路由设计

Pipeline 页面是一个**新路由**，不是 Modal：

```typescript
// ClawWikiShell.tsx 新增路由
<Route path="/connect-wechat" element={<ConnectWeChatPipelinePage />} />
```

流程：
1. 用户点击侧边栏"微信助手 · 未连接"→ 弹 ConnectWeChatModal（已有）
2. 用户选择"一键接入"→ Modal 关闭 → `navigate("/connect-wechat")`
3. Pipeline 页面全屏渲染，左侧进度 + 右侧 webview
4. 完成后显示 Congrats 页面
5. 点击"完成"→ `navigate("/ask")`，侧边栏状态变绿

### 5.3 组件拆分

| 组件 | 职责 |
|------|------|
| `ConnectWeChatPipelinePage.tsx` | 全屏路由页面，管理 Pipeline 状态机 |
| `PipelineProgressBar.tsx` | 顶部 5 步进度条（Replit 风格） |
| `PipelineStepPanel.tsx` | 左侧面板：当前步骤描述 + 已完成列表 + 日志 |
| `PipelineWebview.tsx` | 右侧嵌入浏览器（webview/iframe） |
| `PipelineCongratsPage.tsx` | 完成页 |

### 5.4 Tauri webview vs iframe

| 环境 | 方式 | 能力 |
|------|------|------|
| **Tauri 桌面端** | `<webview>` 标签 | 完整浏览器能力，Cookie 持久化，登录态保持 |
| **浏览器开发模式** | `<iframe>` 降级 | 受限于 X-Frame-Options，Cloudflare/微信可能阻止 |

**降级策略**：如果 iframe 被目标网站阻止，显示提示 + "在浏览器打开" 按钮，
让用户在外部浏览器完成该步骤，Pipeline 侧通过轮询 API 检测完成状态。

---

## 六、与 v1 方案的对比

| 维度 | v1（纯 Modal） | v2（嵌入式浏览器） |
|------|---------------|------------------|
| **用户可见性** | 只看到进度条 + 文字日志 | 实时看到浏览器操作过程 |
| **Cloudflare 注册** | 跳转外部浏览器 | App 内 webview 完成 |
| **微信授权扫码** | Modal 内显示 QR 图片 | webview 直接显示微信后台 |
| **空间利用** | 小 Modal 窗口 | 全屏双栏布局 |
| **信任感** | "后台在做什么？" | "我看到了每一步" |
| **技术复杂度** | 低（纯 React） | 中（需要 webview 集成） |
| **降级能力** | — | iframe 被阻止时降级为外部浏览器 |

---

## 七、实施路线

| 优先级 | 任务 | 依赖 |
|--------|------|------|
| P0 | ConnectWeChatPipelinePage 路由 + 左侧面板 | 已有 Pipeline API |
| P0 | PipelineProgressBar（Replit 风格进度条） | 无 |
| P0 | PipelineWebview 组件（webview/iframe） | MinApp 基础设施 |
| P1 | webview URL 自动切换（跟随 Pipeline 阶段） | Pipeline 状态机 |
| P1 | PipelineCongratsPage（完成页） | 无 |
| P2 | webview 自动化（自动填表、自动检测完成） | Tauri webview API |
| P2 | iframe 降级 + 外部浏览器 fallback | 无 |
